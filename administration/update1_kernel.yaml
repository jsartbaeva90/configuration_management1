---
  - hosts: instance
    tasks:
      - name: update kernel to latest
        when: ansible_distribution == "ubuntu" #and ansible_distrubution_major_version == "18.04"s
        package:
          name: kernel
          state: latest
        become: yes
        become_method: sudo
      - name: restart the system
        when: ansible_distribution == "ubuntu"
        reboot: